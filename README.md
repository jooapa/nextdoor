# *N*ext*D*oor

Going to be for now a CLI tool for syncing folders to nextcloud.

```sh
nextdoor init path/to/folder
nextdoor login 


nextdoor status # compare local and remote
nextdoor pull # newer files from nextcloud
nextdoor push # newer files to nextcloud
nextdoor sync # push and pull
```